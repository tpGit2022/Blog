[TOC]

# 判断变量的数据类型
```
if type(item_list) == list:
    print("is list")
elif type(item_list) == str:
    print("is str")
elif type(item_list) == dict:
    print("is dict")
else:
    print("i don't know what type it is")
if isinstance(item_list, list):
    print("true")
```
# 遍历
